{
  "name": "raml-schema-type",
  "version": "0.0.1",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "An element that transforms JSON schema or any arbitrary JS object to RAML type",
  "devDependencies": {
    "polymer-cli": "^1.2.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "raml-schema-type.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint raml-schema-type.html --rules polymer-1",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"raml-schema-type:local-test\"",
    "test": "node_modules/.bin/polymer test --plugin local",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/raml-schema-type.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/raml-schema-type/issues",
    "email": "arc@mulesoft.com"
  }
}